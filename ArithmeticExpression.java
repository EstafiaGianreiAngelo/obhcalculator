package com.android.obhcalculator;


public class ArithmeticExpression {
	public static int evaluate (String expression, int numberSystem) {
		/*
		 * This method should parse the expression and 
		 * store the operands and operators in separate stacks and 
		 * by using the stacks, the method will calculate the result by 
		 * calling the push and pop methods of the stacks.
		 * 
		 * Hint: You will use Integer.parseInt(String number, int radix) method 
		 * in order to convert numbers from String into int datatype.
		 * */
		
		return 0;	// Temp code. Replace '0' with the actual result.
	}
}
